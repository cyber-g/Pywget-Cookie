# Pywget-Cookie
pywget-cookie is a Python command-line utility that loads active session cookies from web browsers and uses them to perform authenticated downloads ; for short a cookie-aware `wget` clone.
